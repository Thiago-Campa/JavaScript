function suma(n1,n2) {
    let resultado = n1 + n2;
    return resultado;
}

let sumamos = suma(1,2);
console.log("resultado de la primer suma: " + sumamos);

sumamos = suma(10,2);
console.log("resultado de la segunda suma: " + sumamos);